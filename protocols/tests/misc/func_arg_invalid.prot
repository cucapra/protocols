// TODO: Should empty structs be allowed? (currently they aren't)
struct Dummy {
  in a : u1,
}

prot func_arg_invalid<dut: Dummy>(a: u1, b: u1, s: u1) {
  a := b;
  fork();
  c := b;
  step();
  s := 1'b0;
}