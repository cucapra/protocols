struct Counter {
    in a: u64,
    out s: u64,
}

fn simple_for<dut: Counter>(in a: u64, in iters: u8, out s: u64) {
    dut.a := a;
    for iters iterations {
        step();
    }
    assert_eq(dut.s, s);
}
