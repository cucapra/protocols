trace_block: 0
trace_result: FAIL
failure_kind: trace_mismatch
interpreter_trace:
trace {
    reset();
    push(2);
    pop(2);
    idle();
    push(3);
    pop(3);
}

parsed_monitor_trace_candidates:
candidate_monitor_trace: 0
trace {
    reset();
    push(2);
    pop(2);
}

