/target
Cargo.lock
.idea/
test_run/
*.btor
**/.DS_Store
monitor/target
monitor/tests/axi_output.txt
*.json
*.png
*.csv
# Allow benchmark results to be committed for CI regression testing
!benchmark_results/*.csv
# Ignore all dotfiles
.*
# But include the .github directory (CI files)
!.github/
!.github/**/*
# And also include .gitignore + .python-version + Cargo config
!.gitignore
!.python-version
!.cargo

# Verilator / iVerilog artifacts
dump.vcd
a.out
obj_dir/
