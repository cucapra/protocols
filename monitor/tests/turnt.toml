# We run Cargo in quiet mode to prevent build messages from 
# appearing in the output for Turnt tests
[envs.monitor]
command = "cargo run --quiet --package protocols-monitor -- -p {filename} {args}"
output.out = "-"
