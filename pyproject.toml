[project]
name = "protocols"
version = "0.1.0"
description = "Scripts for generating the Protocols test catalog and Runt configs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.1",
    "ty>=0.0.17",
]
