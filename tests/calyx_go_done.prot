// need to implement parsing for struct + input type
fn calyx_go_done(in ii: u32, out oo: u32) {
  dut.ii := ii;
  dut.go := 1;
  while !(dut.done == 1) {
    step();
  }
  dut.go := 0;
  dut.ii := X;
  oo := dut.oo;
}
